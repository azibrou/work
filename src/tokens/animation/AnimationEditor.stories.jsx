import AnimationEditor from './AnimationEditor';

const meta = {
  title: 'Tokens/Animation',
  component: AnimationEditor,
  parameters: { layout: 'padded' },
};

export default meta;

// Edit duration and easing per token, preview with Play, then Apply to write animation.css.
export const Patterns = {};
