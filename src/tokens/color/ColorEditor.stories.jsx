import ColorEditor from './ColorEditor';

const meta = {
  title: 'Tokens/Color',
  component: ColorEditor,
  parameters: { layout: 'padded' },
};

export default meta;

// Edit colors with the picker or hex input, then Apply to write colors.css.
export const Palette = {};
